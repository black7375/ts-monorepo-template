{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "hello",
  "version": "0.0.1",
  "type": "module",
  "typings": "index.d.ts",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "yarn run -T rimraf dist .turbo tsconfig.tsbuildinfo node_modules/.vite",
    "build": "yarn run -T vite build",
    "lint": "yarn run -T eslint .",
    "check": "yarn run -T tsc --noEmit"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:^",
    "tsconfig": "workspace:^"
  }
}
